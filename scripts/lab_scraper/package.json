{
  "name": "tcf-lab-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "SEAS Faculty Directory scraper that feeds the Supabase `labs` table consumed by load_labs.",
  "scripts": {
    "snapshot:filters": "tsx snapshot-filters.ts",
    "scrape:listing": "tsx scrape-listing.ts",
    "scrape:listing:new": "tsx scrape-listing.ts --new",
    "scrape:profiles": "tsx scrape-profiles.ts",
    "scrape:profiles:new": "tsx scrape-profiles.ts --new",
    "diff:labs": "tsx diff-labs.ts",
    "load:supabase": "tsx load-to-supabase.ts",
    "load:supabase:new": "tsx load-to-supabase.ts --new",
    "scrape:all": "npm run scrape:listing && npm run scrape:profiles && npm run load:supabase",
    "scrape:safe": "npm run scrape:listing:new && npm run scrape:profiles:new && npm run diff:labs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "playwright": "^1.47.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
